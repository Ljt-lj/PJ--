# GPU fine-tuning dependencies (install on cloud GPU machine)
# NOTE: Do NOT `pip install torch` here if using ModelScope AMD ROCm images —
#       the image already ships ROCm PyTorch. Installing cu121 will break GPU.
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0
modelscope>=1.14.0
sentencepiece>=0.2.0
protobuf>=3.20.0
tqdm>=4.66.0

# optional experiment tracking (enable with --swanlab)
# swanlab>=0.3.0
